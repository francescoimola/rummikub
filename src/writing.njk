---
layout: _base.njk
title: Drawing my own conclusions | Blog | Francesco Imola
description: Guides, essays and notes on design, copywriting and running a small business. Practical where it helps, honest where it doesn't. Have a read.
eleventyNavigation:
  key: Writing
  parent: Home
---
{% from "_writing-item.njk" import writingItem %}
{% from "_page-header.njk" import pageHeader %}
  {{ pageHeader("meditating", "Drawing my own conclusions", "Yet another blog on the web") }}

  {% for typeSlug in collections.writingTypes %}
  {% set t = writingTypes[typeSlug] %}
  <section class="flow has-gap-m writing-section{% if typeSlug == 'notes' %} writing-section--quiet{% endif %}">
    <div class="flow has-gap-2xs pretty">
      <h2>{{ t.short}}</h2>
      <p class="dimmed">{{ t.blurb }}</p>
    </div>
    <div class="flow has-gap-xs">
      {% for item in collections.writing | byType(typeSlug) | limit(2) %}
        {{ writingItem(item) }}
      {% endfor %}
    </div>
   <a href="/writing/{{ typeSlug }}/" data-icon="arrow-right">See all {{ t.short | capitalize }}</a>
  </section>
  {% endfor %}
