---
import { Heading } from "@radix-ui/themes";

interface Props {
    className?: string;
}

const { className } = Astro.props;
---

<Heading
    as="h1"
    size="8"
    weight="medium"
    highContrast
    trim="both"
    className={className}
>
    <slot />
</Heading>
