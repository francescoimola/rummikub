---
export const prerender = true;
import Layout from "../../layouts/Layout.astro";
import PageHeading from "../../components/PageHeading";
import ProjectImage from "../../components/project/ProjectImage.astro";
import { Text, Flex, Grid, Section, Theme } from "@radix-ui/themes";
import { InnerSection } from "../../components/InnerSection";
import { MainGrid } from "../../components/MainGrid";
import { getAllPublishedProjects } from "../../utils/getAllPublishedProjects";
import { SEO } from "../../constants";
import JsonLd from "../../components/JsonLd.astro";

const projects = await getAllPublishedProjects();
---

<Layout
    title={SEO.pages.work.title}
    description={SEO.pages.work.description}
    backgroundColor="var(--brand-surface-dark-mustard)"
    appearance="dark"
    roleFullWidth
    noMobileRole
>
    <Fragment slot="structured-data">
        <JsonLd
            data={{
                "@type": "CollectionPage",
                name: "Selected Work",
                description: SEO.pages.work.description,
                author: SEO.person,
            }}
        />
    </Fragment>

    <!-- Slot: Main Heading -->
    <PageHeading slot="heading">Selected Work</PageHeading>

    <!-- Slot: Description/Subtitle -->
    <Flex slot="description" direction="column" gap="5">
        <Text size="3"
            >I work in that infamous junction where marketing and design meet and are constantly chasing one another. I’ve been at it for about six years now. I mainly focus on digital marketing and campaigns for ambitious businesses, non-profit organisations and entrepreneurs. Below is a selection of the projects I’ve worked on, ordered by date.
        </Text>
    </Flex>

    <!-- Page Content -->
    <Section slot="page-content" asChild pt="0">
        <Theme
            appearance="light"
            accentColor="orange"
            radius="none"
            grayColor="olive"
            style={{ backgroundColor: "var(--gray-1)" }}
        >
            <MainGrid>
                <InnerSection title="Projects" showCounter={false}>
                    <Grid columns="1" gapY="var(--space-9)" mb="4">
                        {
                            projects.map((project) => (
                                <ProjectImage
                                    src={project.data.coverImage}
                                    alt={project.data.coverAlt}
                                    cardTitle={project.data.title}
                                    skills={project.data.skills ?? []}
                                    href={
                                        project.data.caseStudyStatus
                                            ? undefined
                                            : `/work/${project.data.slug}`
                                    }
                                    caseStudyStatus={
                                        project.data.caseStudyStatus
                                    }
                                />
                            ))
                        }
                    </Grid>
                </InnerSection>
            </MainGrid>
        </Theme>
    </Section>
</Layout>
